import { date_get_millisecond } from '../main'

describe("date_get_millisecond", () => {
  it("main", () => { });
  it("MDN", () => {
    var ms,
      Today = new Date();
    ms = Today.getMilliseconds();
  });
})