import { date_format, date_start } from '../main'

describe("date_start", () => {
  it("main", () => {
    // const date = new Date();
    // console.log(date_start);
    // expect(date_format(date_start(date, 'millisecond'), 'msc')).toEqual('000');
    // expect(date_format(date_start.millisecond(date), 'msc')).toEqual('000');
    // expect(date_format(date_start(date, 'second'), 'ss msc')).toEqual('00 000');
    // expect(date_format(date_start(date, 'minute'), 'mm:ss msc')).toEqual('00:00 000');
    // expect(date_format(date_start(date, 'hour'), 'hh:mm:ss msc')).toEqual('00:00:00 000');
    // expect(date_format(date_start(date, 'day'), 'DD hh:mm:ss msc')).toEqual('01 00:00:00 000');
    // expect(date_format(date_start(date, 'month'), 'MM-DD hh:mm:ss msc')).toEqual('01-01 00:00:00 000');
    // expect(date_format(date_start(date, 'year'))).toEqual('1970-01-01 00:00:00 000');
  });
})