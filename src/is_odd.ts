/**
 * @name is_even
 * @description 检测数值是否为奇数。
 * @category Number
 * @since 0.0.2
 * @param {Number} num 需要检测的数值
 * @returns {Boolean} 检测结果
 */
export const is_odd = (num: number): boolean => num % 2 === 1;
