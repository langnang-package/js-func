/**
 * 解答数独问题
 * 规则：
 * 1. 每行不可重复
 * 2. 每列不可重复
 * 3. 每块不可重复
 */
// TODO
/**
 * solve_sudoku
 * @description 解答数独问题
 * @param {Array} sudoku 
 * @param {Array} unique 
 * @returns {Array} 解答后的数独
 */
export const solve_sudoku = (sudoku: any[], unique: any[] = [1, 2, 3, 4, 5, 6, 7, 8, 9]) => {

}